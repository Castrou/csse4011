
// A very simple protocol definition, consisting of only
// one message.

syntax = "proto2";

message Payload {
    repeatable uint64_t nodeInfo[2] = 1
}

message BaseMessage {
    required uint64_t packetInfo = 1;
    optional Payload payload = 2;
}